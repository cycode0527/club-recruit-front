import { listUsers, type ListRequest, type ListResponse } from "$lib/api/admin";

export async function load() {
    // let request: ListRequest = {
    //     only_applied_users: true,
    //     no_super_users: true,
    //     status: null,
    //     want_direction: null,
    //     vague_real_name: null,
    //     limit: 10,
    //     offset: 0,
    // }
    // let response: ListResponse = await listUsers(request) as ListResponse;
    // return {
    //     message: response.message,
    //     users: response.data,

        
    // };

}